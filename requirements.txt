anthropic>=0.40.0
# --- optional transcription backends (install whichever you use) ---
# assemblyai>=0.30.0      # TRANSCRIPTION_BACKEND=assemblyai (100 hrs/month free)
# openai>=1.0.0           # TRANSCRIPTION_BACKEND=openai     (paid, whisper-large-v3)
# faster-whisper>=1.0.0   # TRANSCRIPTION_BACKEND=local      (on-device, needs ffmpeg)
# yt-dlp>=2024.0.0        # needed by local + openai backends to download audio
feedparser>=6.0.11
requests>=2.31.0
python-dotenv>=1.0.1
APScheduler>=3.10.0
Pillow>=10.0.0
youtube-transcript-api>=0.6.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
PyJWT>=2.8.0
pytest>=8.0.0
httpx>=0.27.0
